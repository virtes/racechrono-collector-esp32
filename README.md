# racechrono-collector-esp32

ESP32-прошивка под PlatformIO для отправки телеметрии в RaceChrono через BLE
DIY CAN-Bus API.

Сейчас проект читает положение дросселя через `SS49E -> ADS1115 A0 -> ESP32`,
давление тормоза через датчик Bosch `0265005303 -> ADS1115 A1 -> ESP32`,
AFR через аналоговый выход контроллера `0-5 В -> делитель 20 кОм/20 кОм ->
ADS1115 A2 -> ESP32`, напряжение АКБ через делитель напряжения на
`ESP32 GPIO34`, а обороты двигателя с первички CDI-катушки через резистор
`47 кОм` и оптрон `PC817 -> ESP32 GPIO27`.
Дроссель публикуется как CAN PID `258`, давление тормоза публикуется как
CAN PID `257`, напряжение АКБ публикуется как CAN PID `259`, обороты двигателя
публикуются как CAN PID `260`, AFR публикуется как CAN PID `261`. Зеленый LED
на `GPIO5` показывает калибровку и состояние BLE-подключения.

## Быстрый старт

```sh
make build
make upload
make monitor
```

Прошить и сразу открыть serial monitor:

```sh
make run
```

Если порт автоопределился неправильно:

```sh
make devices
make run PORT=/dev/cu.wchusbserial10
```

По умолчанию используется окружение `wemos-d1-mini32`. Другое окружение:

```sh
make run ENV=esp32dev PORT=/dev/cu.usbserial-0001
```

## BLE и RaceChrono

BLE-устройство рекламируется как:

```text
RaceExporter
```

RaceChrono setup:

```text
Settings -> Add other device -> Add other device -> RaceChrono DIY -> Bluetooth LE
```

Каналы в Vehicle profile:

| Канал | CAN PID | Equation | Unit |
| --- | --- | --- | --- |
| Brake pressure | `257` | `bytesToUint(raw, 0, 2) / 100.0` | `bar` |
| Throttle position | `258` | `bytesToUint(raw, 0, 2) / 100.0` | `%` |
| Battery voltage | `259` | `bytesToUint(raw, 0, 2) / 1000.0` | `V` |
| Engine RPM | `260` | `bytesToUint(raw, 0, 2)` | `rpm` |
| AFR | `261` | `bytesToUint(raw, 0, 2) / 100.0` | `AFR` |

## LED-индикация

| Состояние | Зеленый LED |
| --- | --- |
| Калибровка закрытого газа | Быстро мигает |
| Пауза перед калибровкой открытого газа | Горит постоянно |
| Калибровка открытого газа | Быстро мигает |
| Обе калибровки успешны | 1 длинное мигание |
| Калибровка закрытого газа не удалась | 2 длинных мигания |
| Калибровка открытого газа не удалась | 3 длинных мигания |
| Рабочий режим, газ открыт больше 1% | Светится с нелинейной PWM-яркостью по уровню открытия газа |
| Рабочий режим, давление тормоза больше 15 бар | Светится с линейной PWM-яркостью от слабого свечения при 15 бар до максимума при 100 бар |
| Рабочий режим, газ и тормоз нажаты одновременно | Светится по более яркому из двух уровней |
| Рабочий режим, газ закрыт и давление тормоза не выше 15 бар, BLE подключен | Короткая вспышка раз в 5 секунд |
| Рабочий режим, газ закрыт и давление тормоза не выше 15 бар, BLE не подключен | Длинная вспышка раз в 2 секунды |

## Схема подключения

Этот раздел является источником правды по текущей проводке. Он специально
оформлен таблицами с устойчивыми ID, чтобы его было удобно читать человеку и
обновлять агенту.

### Компоненты

| ID | Компонент | Назначение |
| --- | --- | --- |
| `esp32` | ESP32 | Основная плата, питание 3.3 В, I2C и BLE |
| `adc` | ADS1115 | Внешний ADC для аналоговых сигналов дросселя, давления тормоза и AFR |
| `throttle_sensor` | SS49E | Датчик открытия дросселя |
| `brake_pressure_sensor` | Bosch 0265005303 | Датчик давления тормозной системы, питание 5 В |
| `afr_controller` | AFR-контроллер | Источник аналогового сигнала AFR: `2.47 В = 15.3 AFR`, `4.764 В = 20 AFR` на выходе контроллера |
| `afr_voltage_divider` | Делитель AFR | `20 кОм` сверху и `20 кОм` снизу; делит аналоговый выход AFR пополам для входа `ADS1115 A2` |
| `ignition_coil` | CDI-катушка зажигания | Источник импульсов оборотов двигателя; постоянного нормального `+12 В` на первичке нет |
| `tach_opto` | PC817 | Оптронная развязка входа оборотов от первички CDI-катушки |
| `tach_input_resistors` | 47 кОм | Входной резистор от импульсного вывода первички CDI к `PC817 pin 1` |
| `tach_input_diode` | 1N4148 или 1N4007 | Защитный диод параллельно входному LED `PC817`, катод к `pin 1`, анод к `pin 2` |
| `tach_pullup_resistor` | Резистор 10 кОм | Подтяжка тахо-входа `ESP32 GPIO27` к `3V3` |
| `boost_converter` | Повышатель напряжения | Питает датчик давления от 1S LiPo АКБ |
| `battery` | 1S LiPo АКБ 3.7 В | Источник питания повышателя напряжения, до 4.2 В при полной зарядке |
| `battery_voltage_divider` | Делитель напряжения АКБ | `100 кОм` сверху и `100 кОм` снизу; делит напряжение 1S LiPo примерно пополам для входа `ESP32 GPIO34` |
| `status_led` | Зеленый LED | Индикатор калибровки и BLE |
| `status_led_resistor` | Резистор 470 Ом | Ограничивает ток светодиода |

### Соединения

| ID | Откуда | Куда | Сигнал / шина | Примечание |
| --- | --- | --- | --- | --- |
| `led-gpio` | `ESP32 GPIO5` | `470 Ом -> LED анод` | `status_led` | GPIO управляет светодиодом через резистор |
| `led-gnd` | `LED катод` | `GND` | `gnd` | Катод светодиода на общую землю |
| `throttle-signal` | `SS49E OUT` | `ADS1115 A0` | `throttle_adc` | Аналоговый сигнал положения дросселя |
| `ss49e-power` | `ESP32 3V3` | `SS49E VCC` | `3v3` | Питание датчика от 3.3 В |
| `ss49e-gnd` | `SS49E GND` | `GND` | `gnd` | Общая земля |
| `boost-in-plus` | `АКБ +` | `Повышатель IN+` | `battery_plus` | Повышатель подключен к 1S LiPo АКБ напрямую |
| `boost-in-minus` | `АКБ -` | `Повышатель IN-` | `gnd` | Минус АКБ должен быть общей землей схемы |
| `brake-sensor-power` | `Повышатель OUT+` | `Bosch 0265005303 pin 3 (+)` | `5v_boost` | Питание датчика давления, замерено 5.11 В |
| `brake-sensor-gnd` | `Повышатель OUT-` | `Bosch 0265005303 pin 1 (-)` | `gnd` | Минус датчика давления на общую землю |
| `brake-sensor-signal` | `Bosch 0265005303 pin 2` | `ADS1115 A1` | `brake_pressure_adc` | 0.613 В = 0 бар, 4.446 В = 250 бар при питании 5.11 В |
| `afr-signal-divider-plus` | `AFR analog out 0-5 В` | `Rверх 20 кОм` | `afr_signal_raw` | Это именно аналоговый выход контроллера AFR, не его питание |
| `afr-signal` | `Средняя точка Rверх/Rниз` | `ADS1115 A2` | `afr_adc` | `Rверх = 20 кОм`, `Rниз = 20 кОм`; 4.764 В на выходе AFR дает около 2.382 В на `A2` |
| `afr-signal-divider-gnd` | `Rниз 20 кОм` | `GND` | `gnd` | Нижнее плечо делителя AFR на общую землю |
| `afr-gnd` | `AFR GND / signal GND` | `GND` | `gnd` | Земля AFR-контроллера должна быть общей с ESP32/ADS1115 |
| `tach-cdi-pulse` | `Импульсный вывод первички CDI-катушки` | `47 кОм -> PC817 pin 1` | `tach_coil_input` | Входной ток LED оптрона ограничен одним резистором 47 кОм |
| `tach-cdi-return` | `Второй вывод первички CDI-катушки / возврат CDI` | `PC817 pin 2` | `tach_coil_input` | Сторона CDI остается гальванически развязанной; не соединять с `ESP32 GND` |
| `tach-input-diode` | `1N4148/1N4007 катод` | `PC817 pin 1` | `tach_coil_input` | Диод стоит параллельно входному LED оптрона, полоской к `pin 1` |
| `tach-input-diode-return` | `1N4148/1N4007 анод` | `PC817 pin 2` | `tach_coil_input` | Защищает входной LED `PC817` от обратного напряжения |
| `tach-pullup` | `ESP32 3V3` | `10 кОм -> ESP32 GPIO27` | `engine_rpm_input` | Внешняя подтяжка тахо-входа к 3.3 В |
| `tach-signal` | `ESP32 GPIO27` | `PC817 pin 4` | `engine_rpm_input` | В коде `kTachInputPin = 27`, чтение по спадающему фронту |
| `tach-opto-gnd` | `PC817 pin 3` | `ESP32 GND` | `gnd` | Эмиттер выходного транзистора оптрона на землю ESP32 |
| `battery-divider-plus` | `АКБ +` | `Rверх 100 кОм` | `battery_plus` | Верхнее плечо делителя; напряжение 1S LiPo до делителя обычно 3.0-4.2 В |
| `battery-divider-signal` | `Средняя точка Rверх/Rниз` | `ESP32 GPIO34` | `battery_voltage_adc` | `Rверх = 100 кОм`, `Rниз = 100 кОм`; используется коэффициент `1.951091`, при 4.2 В АКБ на `GPIO34` около 2.1 В |
| `battery-divider-gnd` | `Rниз 100 кОм` | `GND` | `gnd` | Нижнее плечо делителя на общую землю |
| `ads-power` | `ESP32 3V3` | `ADS1115 VDD` | `3v3` | Питание ADS1115 от 3.3 В |
| `ads-gnd` | `ADS1115 GND` | `GND` | `gnd` | Общая земля |
| `ads-scl` | `ESP32 GPIO19` | `ADS1115 SCL` | `i2c_scl` | I2C clock, в коде `kAdcSclPin = 19` |
| `ads-sda` | `ESP32 GPIO22` | `ADS1115 SDA` | `i2c_sda` | I2C data, в коде `kAdcSdaPin = 22` |
| `ads-addr` | `ADS1115 ADDR` | `GND` | `i2c_addr` | Адрес ADS1115: `0x48` |

### Логические шины

| Шина | Что объединяет |
| --- | --- |
| `gnd` | `ESP32 GND`, `ADS1115 GND`, `SS49E GND`, `AFR GND / signal GND`, `АКБ -`, `Повышатель IN-/OUT-`, `Bosch 0265005303 pin 1 (-)`, катод LED |
| `3v3` | `ESP32 3V3`, `ADS1115 VDD`, `SS49E VCC` |
| `5v_boost` | `Повышатель OUT+`, `Bosch 0265005303 pin 3 (+)` |
| `battery_plus` | `АКБ +` 1S LiPo, `Повышатель IN+` |
| `battery_voltage_adc` | `Делитель напряжения АКБ средняя точка -> ESP32 GPIO34` |
| `afr_adc` | `AFR analog out 0-5 В -> 20 кОм -> ADS1115 A2`, `ADS1115 A2 -> 20 кОм -> GND` |
| `i2c` | `ESP32 GPIO19 -> ADS1115 SCL`, `ESP32 GPIO22 -> ADS1115 SDA` |
| `engine_rpm_input` | `ESP32 GPIO27`, `10 кОм pull-up к 3V3`, `PC817 pin 4`; `PC817 pin 3 -> ESP32 GND` |
| `tach_coil_input` | `Импульсный вывод первички CDI-катушки -> 47 кОм -> PC817 pin 1`, `второй вывод первички CDI-катушки / возврат CDI -> PC817 pin 2`, защитный диод между `PC817 pin 1` и `pin 2` |

### Важно

- Все `GND` низковольтной схемы должны быть объединены. Сторона CDI
  тахо-входа (`первичка CDI-катушки`, вход `PC817 pin 1/2`) остается
  гальванически развязанной от `ESP32 GND`.
- ADS1115 и SS49E питаются от `3V3`, не от `5V`.
- Датчик давления Bosch `0265005303` питается от выхода повышателя `5V`;
  текущее замеренное питание для расчета давления: `5.11 В`.
- Аналоговый выход AFR `0-5 В` подключается к `ADS1115 A2` только через
  делитель `20 кОм / 20 кОм`. В прошивке напряжение выхода AFR считается как
  `afr_signal_volts = A2_volts * 2.0`, затем AFR считается линейно по
  напряжению на выходе контроллера: `2.47 В = 15.3 AFR`,
  `4.764 В = 20 AFR`.
- `AFR GND / signal GND` должен быть подключен к общей земле ESP32/ADS1115.
  Без общей земли `ADS1115 A2` не имеет правильной точки отсчета для сигнала.
- Первичку CDI-катушки нельзя подключать напрямую к `ESP32`, `ADS1115` или
  общей земле схемы. Вход оборотов подключается только через `PC817`.
- На входе `PC817` для оборотов используется один резистор `47 кОм` от
  импульсного вывода первички CDI-катушки к `PC817 pin 1`; `PC817 pin 2`
  подключается ко второму выводу первички CDI-катушки / возврату CDI.
- Защитный диод `1N4148` или `1N4007` ставится параллельно входному LED
  `PC817`: катод, сторона с полоской, к `PC817 pin 1`, анод к `PC817 pin 2`.
- Выход `PC817` для оборотов: `PC817 pin 4 -> ESP32 GPIO27`, `PC817 pin 3 ->
  ESP32 GND`, подтяжка `10 кОм` от `GPIO27` к `ESP32 3V3`.
- В прошивке обороты читаются по `GPIO27` по спадающему фронту. RPM считается
  гибридно: медиана последних 3 периодов, оконный счетчик импульсов на высоких
  оборотах и ограничение сверху по времени с последнего импульса, чтобы после
  сброса газа значение не подвисало. Коэффициент
  `kIgnitionPulsesPerRevolution = 1.0` рассчитан на 4-тактный 1-цилиндровый
  двигатель с одним импульсом за один оборот коленвала. Если зажигание дает
  один импульс за два оборота, нужно изменить коэффициент на `0.5` в
  `src/main.cpp`.
- Сигнал на `ADS1115 A1` не должен превышать допустимый уровень входа ADC.
  Если выход датчика может подниматься выше питания ADS1115, нужен делитель
  напряжения или другой согласователь уровня.
- Напряжение 1S LiPo АКБ подключается к `ESP32 GPIO34` только через делитель.
  Текущий делитель: `100 кОм` от `АКБ +` до `GPIO34` и `100 кОм` от
  `GPIO34` до `GND`. Он рассчитан примерно на деление пополам: полностью
  заряженная LiPo 4.2 В дает на `GPIO34` около 2.1 В. В коде используется
  откалиброванный коэффициент `1.951091`, поэтому расчет такой:
  `battery_volts = GPIO34_volts * 1.951091`.
- `GPIO34` используется как вход встроенного `ADC1` ESP32. Это input-only пин,
  поэтому он подходит для измерения напряжения и не должен использоваться как
  цифровой выход.
- Для более спокойных показаний можно поставить конденсатор `100 нФ` между
  `GPIO34` и `GND` рядом с ESP32.
- В лог и RaceChrono отправляется сглаженное напряжение АКБ: EMA-фильтр с
  коэффициентом `0.2` уменьшает скачки от шума и кратких просадок.
- `ADS1115 A2` используется для AFR, `ADS1115 A3` свободен.
- LED подключается только через резистор 470 Ом, не напрямую к GPIO.
